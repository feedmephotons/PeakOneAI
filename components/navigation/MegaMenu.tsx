'use client'

// MegaMenu is no longer used in the top navigation.
// Core navigation items are rendered directly in Navigation.tsx.
// Power features are accessible via CreateMenu and the command bar (Cmd+K).
// This component is kept as a no-op export for backward compatibility.

export default function MegaMenu() {
  return null
}
